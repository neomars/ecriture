{
  "scripts": {
    "postinstall": "npm install --prefix ecriture",
    "tauri": "npm run tauri --prefix ecriture --",
    "start": "npm run start --prefix ecriture",
    "package:linux": "npm run package:linux --prefix ecriture",
    "package:windows": "npm run package:windows --prefix ecriture",
    "package:macos": "npm run package:macos --prefix ecriture"
  },
  "dependencies": {
    "jsdom": "^29.1.1",
    "playwright": "^1.63.0"
  }
}
